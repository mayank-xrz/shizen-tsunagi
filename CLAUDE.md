Read `memory.md` first, then `rules.md` (ponytail full — binding), then `phased.md`; `design.md` is the visual authority and wins over `prd.md`.
Files: only what architecture.md §2 lists, plus the two owner-approved root additions — `notify.check.mjs` and this file. Nothing else.
Runtime deps are exactly `next`, `react`, `react-dom`, `resend`. When blocked on anything only the owner can supply, ask in **bold** — never guess, never ask for secrets in chat.
At session end: append to `memory.md` (decisions, phase status, session note) and commit.
